{
  "name": "khatabill",
  "version": "1.0.0",
  "description": "KhataBill — GST billing, expense and ledger management API with WhatsApp commands",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "node -e \"require('./config/db').query(require('fs').readFileSync('./migrations/001_init.sql', 'utf8')).then(() => { console.log('Migration complete'); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "https-proxy-agent": "^7.0.4",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
